# Copy to backend/.env and fill in (never commit .env).

# Anthropic (Claude) — agent (4. način pretrage). https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional overrides
# AGENT_MODEL=claude-haiku-4-5
# AGENT_SEARCH_LIMIT=10
